# Опционально: полный URL бэкенда, если фронт и API на разных хостах (без завершающего /).
# Пусто = запросы на тот же origin, путь /api/... (как в docker-compose с nginx).
# VITE_API_ORIGIN=https://api.example.com
